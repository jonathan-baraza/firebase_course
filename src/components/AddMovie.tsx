import React from "react";

function AddMovie() {
  return (
    <div
      className="fixed inset-0 flex items-center justify-center"
      style={{ backgroundColor: "rgba(0,0,0,0.5)" }}
    >
      <div className="bg-white">Add New Movie</div>
    </div>
  );
}

export default AddMovie;
